# Contributing

1. Run all GoogleTest units (`ctest --test-dir build_release`) before submitting a pull request.
2. Adhere to the existing C++ coding standard. Avoid unstructured comments.
3. Ensure the simulator logic remains detached from real-time OS dependencies.
